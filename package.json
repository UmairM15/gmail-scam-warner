{
  "name": "gmail-scam-warner",
  "version": "1.0.0",
  "description": "Chrome Extension — warns users about suspicious emails in Gmail.",
  "private": true,
  "scripts": {
    "build": "esbuild src/content.ts --bundle --outfile=dist/content.js --platform=browser --target=chrome112 --sourcemap",
    "watch": "esbuild src/content.ts --bundle --outfile=dist/content.js --platform=browser --target=chrome112 --sourcemap --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
